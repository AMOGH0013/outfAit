"""
Offline evaluation harness for the wardrobe recommender.

Important: Nothing in `app/` should import from `evaluation/`.
Run scripts from the repo root, e.g.:

  python -m evaluation.simulate_users --days 21
  python -m evaluation.report
"""

